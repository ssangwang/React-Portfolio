import React from "react";

const styles = {
    heading: {
      margin: '20px'
    }
  };

export default function Header(){

return (
    <div>
        <h1 style={styles.heading}>Portfolio</h1>
    </div>
)
    
}