import React from "react";

const styles = {
    heading: {
      margin: '20px',
      textAlign: 'center',
    }
  };

export default function Header(){

return (
    <div>
        <h1 style={styles.heading}>React Portfolio</h1>
    </div>
)
    
}